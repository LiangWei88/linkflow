# 开发计划

**版本**: 1.0\
**日期**: 2026-03-21\
**周期**: 8 周（MVP 阶段）

***

## 1. 项目概述

### 1.1 开发目标

实现双链大纲笔记软件 MVP，包含大纲编辑器、双向链接、Daily Notes、自托管部署等核心功能。

### 1.2 开发原则

| 原则    | 说明                 |
| ----- | ------------------ |
| 轻量级分层 | 避免过度设计，保留扩展性       |
| 前后端分离 | Express 服务端 + 前端 API 客户端 |
| 渐进增强  | MVP 核心功能完整，预留扩展接口  |
| 快速迭代  | 优先完成核心功能，后续优化      |

***

## 2. 技术架构

### 2.1 分层设计

```
┌─────────────────────────────────────────┐
│                 UI 层                    │
│         React + Tiptap                  │
│         界面渲染、用户交互               │
├─────────────────────────────────────────┤
│              前端应用层                   │
│         TypeScript Services             │
│         业务逻辑、API 客户端             │
├─────────────────────────────────────────┤
│              服务端层                    │
│         Express + Node.js               │
│         RESTful API、业务逻辑           │
├─────────────────────────────────────────┤
│                数据层                    │
│         SQLite + better-sqlite3         │
│         数据存储、查询                   │
├─────────────────────────────────────────┤
│                存储层                    │
│         文件系统                         │
│         SQLite 文件 + 附件存储           │
└─────────────────────────────────────────┘
```

### 2.2 技术选型

| 类别  | 技术             | 版本  | 说明             |
| --- | -------------- | --- | -------------- |
| 框架  | React          | 18+ | UI 组件化开发       |
| 编辑器 | Tiptap         | 2.x | 基于 ProseMirror |
| 构建  | Vite           | 5.x | 快速开发构建         |
| 服务端 | Express        | 4.x | RESTful API 服务 |
| 数据库 | SQLite         | 3.x | 嵌入式数据库         |
| 驱动  | better-sqlite3 | 9.x | 同步 API         |
| 类型  | TypeScript     | 5.x | 类型安全           |
| 样式  | Tailwind CSS   | 3.x | 原子化 CSS        |
| 拖拽  | @dnd-kit       | 6.x | 现代拖拽库          |
| 日期  | date-fns       | 3.x | 日期处理           |
| 部署  | Docker         | -   | 容器化部署          |

***

## 3. 开发里程碑

### 3.1 里程碑总览

```
Week 1-2: 基础架构
├── 项目骨架搭建
├── 数据层实现
└── Tiptap 集成

Week 3-4: 核心功能
├── 大纲编辑器
└── 双向链接

Week 5-6: 功能完善
├── Daily Notes
├── 搜索功能
└── 反链面板

Week 7-8: 部署优化
├── Docker 部署
├── PWA 适配
└── 测试与修复
```

### 3.2 详细计划

| 阶段       | 任务            | 时间       | 优先级   | 交付物               |
| -------- | ------------- | -------- | ----- | ----------------- |
| **阶段 1** | 项目骨架与数据层      | Week 1   | ★★★★★ | SQLite 初始化、表结构    |
| **阶段 2** | UI 层集成 Tiptap | Week 1-2 | ★★★★★ | 编辑器渲染、Markdown 支持 |
| **阶段 3** | 大纲编辑器         | Week 2-3 | ★★★★★ | 层级结构、折叠/展开、拖拽     |
| **阶段 4** | 双向链接          | Week 3-4 | ★★★★★ | Wikilink、反链面板     |
| **阶段 5** | Daily Notes   | Week 5   | ★★★★☆ | 自动创建、模板系统         |
| **阶段 6** | 搜索与导航         | Week 5-6 | ★★☆☆☆ | 全局搜索、快速跳转         |
| **阶段 7** | Docker 部署     | Week 6-7 | ★★★☆☆ | 容器化、数据持久化         |
| **阶段 8** | PWA 适配        | Week 7-8 | ★☆☆☆☆ | 安装为 App、触控优化      |

***

## 4. 详细开发步骤

### 4.1 阶段 1: 项目骨架与数据层

**时间**: Week 1\
**优先级**: ★★★★★

#### 任务清单

- [x] 初始化项目结构
- [x] 配置 TypeScript + Vite
- [x] 配置 ESLint + Prettier
- [x] 初始化 SQLite 数据库
- [x] 定义表结构（blocks, backlinks）
- [x] 实现 BlockRepository
- [x] 实现 BlockService
- [x] 实现 Express 服务端
- [x] 创建前端 API 客户端
- [x] 编写单元测试

#### 目录结构

```
/
├── server.ts              # Express 服务端
├── src/
│   ├── api/
│   │   └── client.ts      # 前端 API 客户端
│   ├── db/
│   │   ├── index.ts       # 数据库连接
│   │   ├── schema.ts      # 表结构定义
│   │   └── migrations/    # 迁移脚本
│   ├── repositories/
│   │   └── BlockRepository.ts # 数据访问层
│   ├── services/
│   │   └── BlockService.ts    # 业务逻辑层
│   └── types/
│       └── block.ts           # 类型定义
```

#### 验收标准

- SQLite 数据库初始化成功
- 表结构正确（blocks, backlinks, blocks\_fts）
- Repository 和 Service 单元测试通过
- Express 服务端启动成功
- API 接口正常响应
- 前端 API 客户端能正确调用后端接口

***

### 4.2 阶段 2: UI 层集成 Tiptap

**时间**: Week 1-2\
**优先级**: ★★★★★

#### 任务清单

- [x] 安装 Tiptap 依赖
- [x] 创建 Editor 组件
- [x] 配置 StarterKit 扩展
- [x] 实现 Markdown 渲染
- [x] 连接 BlockService 加载数据
- [x] 实现自动保存

#### 核心代码

```typescript
// components/Editor.tsx
import { useEditor, EditorContent } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';

export function Editor({ docId }: { docId: string }) {
  const editor = useEditor({
    extensions: [StarterKit],
    content: '',
    onUpdate: ({ editor }) => {
      // 自动保存
      debouncedSave(editor.getJSON());
    },
  });
  
  return <EditorContent editor={editor} />;
}
```

#### 验收标准

- Tiptap 编辑器渲染成功
- 可输入和编辑 Markdown
- 内容从 API 加载并显示
- 自动保存功能正常
- 与服务端 API 通信正常

***

### 4.3 阶段 3: 大纲编辑器

**时间**: Week 2-3\
**优先级**: ★★★★★

#### 任务清单

- [ ] 实现 OutlineItem Node 扩展
- [ ] 实现层级嵌套结构
- [ ] 实现折叠/展开功能
- [ ] 集成 @dnd-kit 拖拽
- [ ] 实现拖拽排序逻辑
- [ ] 更新数据库 order 字段

#### 核心代码

```typescript
// extensions/OutlineItem.ts
import { Node, wrappingInputRule } from '@tiptap/core';

export const OutlineItem = Node.create({
  name: 'outlineItem',
  content: 'paragraph outlineItem*',
  defining: true,
  
  parseHTML() {
    return [{ tag: 'li[data-type="outline-item"]' }];
  },
  
  renderHTML({ HTMLAttributes }) {
    return ['li', { ...HTMLAttributes, 'data-type': 'outline-item' }, 0];
  },
});
```

#### 验收标准

- 可创建多级大纲结构
- 折叠/展开功能正常
- 拖拽排序后顺序正确
- 数据库正确存储层级关系

***

### 4.4 阶段 4: 双向链接

**时间**: Week 3-4\
**优先级**: ★★★★★

#### 任务清单

- [ ] 实现 Wikilink Mark 扩展
- [ ] 实现输入 `[[` 触发补全
- [ ] 实现补全菜单组件
- [ ] 实现链接点击跳转
- [ ] 实现 LinkService
- [ ] 实现反链存储逻辑
- [ ] 创建反链面板组件
- [ ] 实现反链拖拽到正文

#### 核心代码

```typescript
// services/LinkService.ts
export class LinkService {
  async syncBacklinks(blockId: string, content: string): Promise<void> {
    const links = this.parseWikilinks(content);
    const existing = await this.getOutgoingLinks(blockId);
    
    // 删除不再存在的链接
    for (const link of existing) {
      if (!links.includes(link)) {
        await this.removeLink(blockId, link);
      }
    }
    
    // 添加新链接
    for (const link of links) {
      if (!existing.includes(link)) {
        await this.createLink(blockId, link);
      }
    }
  }
}
```

#### 验收标准

- 输入 `[[` 时弹出补全菜单
- 点击链接可跳转到目标块
- 反链数据正确存储
- 反链面板展示正确

***

### 4.5 阶段 5: Daily Notes

**时间**: Week 5\
**优先级**: ★★★★☆

#### 任务清单

- [ ] 实现 DailyNoteService
- [ ] 实现自动创建逻辑
- [ ] 实现模板系统
- [ ] 实现日期导航
- [ ] 实现快捷键 `Ctrl+K`
- [ ] 实现附件上传

#### 核心代码

```typescript
// services/DailyNoteService.ts
export class DailyNoteService {
  async getOrCreateTodayNote(): Promise<DailyNote> {
    const today = format(new Date(), 'yyyy-MM-dd');
    const existing = await this.blockService.getBlocks(today);
    
    if (existing.length > 0) {
      return { date: today, block: existing[0], isNew: false };
    }
    
    const template = await this.getTemplate('default');
    const content = this.renderTemplate(template, { date: today });
    const block = await this.blockService.createBlock({
      docId: today,
      content,
    });
    
    return { date: today, block, isNew: true };
  }
}
```

#### 验收标准

- 每日启动时自动创建笔记
- 模板内容正确加载
- 日期导航功能正常
- 快捷键可快速插入链接

***

### 4.6 阶段 6: 搜索与导航

**时间**: Week 5-6\
**优先级**: ★★☆☆☆

#### 任务清单

- [ ] 创建 FTS5 全文索引
- [ ] 实现 SearchService
- [ ] 创建搜索面板组件
- [ ] 实现搜索结果高亮
- [ ] 实现 `Ctrl+P` 快速跳转
- [ ] 实现最近访问记录

#### 核心代码

```typescript
// services/SearchService.ts
export class SearchService {
  async search(keyword: string, limit = 20): Promise<SearchResult[]> {
    const results = this.db.prepare(`
      SELECT b.*, fts.rank
      FROM blocks b
      JOIN blocks_fts fts ON b.id = fts.rowid
      WHERE fts.content MATCH ?
      ORDER BY fts.rank
      LIMIT ?
    `).all(keyword, limit);
    
    return results.map(r => ({
      block: this.toBlock(r),
      highlights: this.extractHighlights(r.content, keyword),
      rank: r.rank,
    }));
  }
}
```

#### 验收标准

- 搜索结果实时展示
- 匹配内容高亮显示
- `Ctrl+P` 可快速跳转
- 搜索响应时间 < 100ms

***

### 4.7 阶段 7: Docker 部署

**时间**: Week 6-7\
**优先级**: ★★★☆☆

#### 任务清单

- [ ] 编写 Dockerfile
- [ ] 编写 docker-compose.yml
- [ ] 配置数据持久化
- [ ] 配置 Nginx 反向代理
- [ ] 编写部署文档
- [ ] 测试一键部署

#### 核心配置

```yaml
# docker-compose.yml
version: '3.8'
services:
  linkflow:
    build: .
    ports:
      - "3000:80"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
```

#### 验收标准

- 可通过 `docker-compose up` 启动
- Web 端访问正常
- 数据持久化，重启不丢失

***

### 4.8 阶段 8: PWA 适配

**时间**: Week 7-8\
**优先级**: ★☆☆☆☆

#### 任务清单

- [ ] 配置 manifest.json
- [ ] 配置 Service Worker
- [ ] 实现离线缓存
- [ ] 优化触控交互
- [ ] 响应式布局适配
- [ ] 测试移动端功能

#### 核心配置

```json
// public/manifest.json
{
  "name": "LinkFlow",
  "short_name": "Notes",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#3b82f6",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

#### 验收标准

- 可通过浏览器"安装"到主屏
- 触控操作流畅
- 移动端布局合理

***

## 5. 测试计划

### 5.1 测试策略

| 类型     | 工具                    | 覆盖目标                 |
| ------ | --------------------- | -------------------- |
| 单元测试   | Jest                  | Services, Utils      |
| 组件测试   | React Testing Library | UI 组件                |
| 集成测试   | Jest                  | Repository + Service |
| E2E 测试 | Cypress               | 关键用户流程               |

### 5.2 覆盖率目标

- Services: 90%
- Utils: 90%
- Components: 70%
- 整体: 80%

详见 [测试计划文档](../docs/测试计划.md)

***

## 6. 风险管理

### 6.1 风险清单

| 风险          | 可能性 | 影响 | 应对措施          |
| ----------- | --- | -- | ------------- |
| SQLite 性能瓶颈 | 中   | 高  | 添加索引、监控查询性能   |
| Tiptap 复杂度高 | 中   | 中  | 使用现成扩展、逐步定制   |
| 移动端适配困难     | 高   | 中  | PWA + 触控优化库   |
| 开发时间超期      | 中   | 高  | 优先核心功能、砍掉次要功能 |

### 6.2 应对策略

1. **每周复盘**: 检查进度，调整优先级
2. **功能裁剪**: 非核心功能可延后
3. **技术债务**: 记录待优化项，后续迭代处理

***

## 7. 发布计划

### 7.1 MVP 发布

**时间**: Week 8 结束

**包含功能**:

- 大纲编辑器（完整）
- 双向链接（完整）
- Daily Notes（完整）
- 搜索功能（基础）
- Docker 部署（完整）
- PWA 适配（基础）

### 7.2 后续迭代

| 版本   | 功能        | 时间   |
| ---- | --------- | ---- |
| V1.1 | 插件系统、主题定制 | +4 周 |
| V1.2 | 多用户、协同编辑  | +8 周 |

***

## 附录 A: 开发环境配置

### 依赖安装

```bash
# 安装依赖
npm install

# 启动服务端
npx tsx server.ts

# 启动前端开发服务器
npm run dev

# 运行测试
npm run test

# 构建生产版本
npm run build
```

### 环境变量

```env
# .env.local
VITE_DATA_DIR=./data
VITE_APP_TITLE=LinkFlow
```

## 附录 B: 相关文档

- [需求文档](./需求文档.md)
- [架构设计](../docs/架构设计.md)
- [数据库设计](../docs/数据库设计.md)
- [API 接口文档](../docs/API接口文档.md)
- [开发规范](../docs/开发规范.md)
- [测试计划](../docs/测试计划.md)
- [部署文档](../docs/部署文档.md)

